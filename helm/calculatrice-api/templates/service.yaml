apiVersion: v1
kind: Service
metadata:
  name: {{ include "calculatrice-api.fullname" . }}
  labels:
    {{- include "calculatrice-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "calculatrice-api.selectorLabels" . | nindent 4 }}
