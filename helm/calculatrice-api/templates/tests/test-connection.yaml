apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calculatrice-api.fullname" . }}-test-connection"
  labels:
    {{- include "calculatrice-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "calculatrice-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
